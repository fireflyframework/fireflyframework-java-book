> _Draft in progress. This chapter belongs to a later plan in the build-out; the line below states its intended scope._

The reactive YAML-DSL rule engine — lexer/parser/AST/visitor, batch scoring, stored rules, audit trail — framed as where automated credit decisioning *belongs*, contrasted honestly with the slice's hand-coded heuristic.
