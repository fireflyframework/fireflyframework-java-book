> _Draft in progress. This chapter belongs to a later plan in the build-out; the line below states its intended scope._

Raise and route domain events with `@EventPublisher`/`@EventListener`, `PublisherType.AUTO`, and `EventEnvelope` — in-JVM first, then over Kafka — plus the CQRS↔EDA bridge.
