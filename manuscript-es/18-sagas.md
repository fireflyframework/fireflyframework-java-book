> _Draft in progress. This chapter belongs to a later plan in the build-out; the line below states its intended scope._

The headline `RegisterApplicationSaga`: a root step with parallel `dependsOn` fan-out, `ExpandEach` per-item expansion, optional skipped steps, and compensation on failure via `SagaEngine`.
