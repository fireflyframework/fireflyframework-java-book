> _Draft in progress. This chapter belongs to a later plan in the build-out; the line below states its intended scope._

Promote the anemic row into a rich `LoanApplication` aggregate with a `Money` value object, invariants, and mappers that isolate the domain from persistence.
