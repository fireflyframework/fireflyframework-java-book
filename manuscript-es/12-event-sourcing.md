> _Draft in progress. This chapter belongs to a later plan in the build-out; the line below states its intended scope._

Optional: an event-sourced ledger with `AggregateRoot`, the R2DBC event store, optimistic concurrency, projections, snapshots, and the transactional outbox.
