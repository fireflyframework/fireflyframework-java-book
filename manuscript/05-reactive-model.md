> _Draft in progress. This chapter belongs to a later plan in the build-out; the line below states its intended scope._

The keystone chapter: Mono and Flux from first principles with StepVerifier, operators, error/retry, schedulers, and the `Hooks.enableAutomaticContextPropagation()` linchpin — taught before any framework feature relies on it.
