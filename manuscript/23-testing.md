> _Draft in progress. This chapter belongs to a later plan in the build-out; the line below states its intended scope._

The test pyramid: `@SpringBootTest` with `WebTestClient` and no socket, Testcontainers for Postgres/Kafka, `StepVerifier`, and saga compensation tests.
