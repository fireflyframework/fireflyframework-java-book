> _Draft in progress. This chapter belongs to a later plan in the build-out; the line below states its intended scope._

The unified resilient client: `ClientFactory`, Resilience4j, `X-Transaction-Id` propagation, and *beyond REST* — SOAP/gRPC/GraphQL/WebSocket through one fluent API.
