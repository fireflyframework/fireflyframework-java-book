> _Draft in progress. This chapter belongs to a later plan in the build-out; the line below states its intended scope._

The data tier (`starter-data`): `DataEnricher`/`EnricherOperation` with provider fallback chains, data-quality gates, and lineage — the home for credit-bureau enrichment, with an honest note on the slice's current bureau stub.
