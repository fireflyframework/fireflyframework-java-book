> _Draft in progress. This chapter belongs to a later plan in the build-out; the line below states its intended scope._

Split writes from reads with `@CommandHandlerComponent`/`@QueryHandlerComponent` over the command and query buses; the multi-tenant `ExecutionContext`; query caching. The domain tier appears.
