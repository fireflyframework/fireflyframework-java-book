> _Draft in progress. This chapter belongs to a later plan in the build-out; the line below states its intended scope._

The four-tier model — experience, domain, core, data — why each tier picks its starter, and SDK-over-HTTP communication that never shares persistence.
