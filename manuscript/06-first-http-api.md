> _Draft in progress. This chapter belongs to a later plan in the build-out; the line below states its intended scope._

A reactive `@RestController` with finance-grade validators (`@ValidIBAN`, `@ValidAmount`), RFC 7807 problem details, and the web module's cross-cutting surface: idempotency filter, PII masking, CORS, request logging.
