> _Draft in progress. This chapter belongs to a later plan in the build-out; the line below states its intended scope._

Generate the loan-agreement PDF with `TemplateRenderUtil`, schedule work with `@ScheduledSaga`, and reach the outside world with notifications, inbound webhooks, and outbound HMAC-signed callbacks.
